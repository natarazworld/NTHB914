CREATE TABLE "SYSTEM"."SUPERMARKET_MEMBERSHIP" 
   (	"MID" NUMBER(10,0) NOT NULL ENABLE, 
	"NAME" VARCHAR2(20 BYTE), 
	"ADDRS" VARCHAR2(20 BYTE), 
	"REWARDPOINTS" NUMBER(10,0), 
	 CONSTRAINT "SUPERMARKET_MEMBERSHIP_PK" PRIMARY KEY ("MID"))